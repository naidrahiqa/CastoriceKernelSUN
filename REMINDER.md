# ⏰ Reminder — Fixes Applied (16 Mei 2026)

## ✅ Critical Fixes

### 1. `KSU_METHOD` Sekarang di-Export ke `GITHUB_ENV`
- **File:** `.github/workflows/_build_kernel_core.yml:423`
- **Masalah:** Step Setup KernelSU define `KSU_METHOD` sebagai local variable, step Setup SUSFS yang beda shell ga bisa akses. Akibatnya SUSFS masuk branch patch yang salah → compile error / bootloop.
- **Fix:** Ditambah `echo "KSU_METHOD=${KSU_METHOD}" >> $GITHUB_ENV` biar lintas step bisa akses.

### 2. Azure Toolchain Dihapus dari Manager
- **File:** `.github/workflows/build_manager_gki.yml`
- **Masalah:** Checkbox `tc_azure_latest` masih ada di Manager tapi gak ada handler di Core workflow. Kalau dicentang → build langsung fail (`exit 1`).
- **Fix:** Hapus input `tc_azure_latest` + handler Python-nya.

## ✅ Consistency Fixes

### 3. LTO: Workflow ↔ Guidelines Sekarang Sinkron
- **File:** `_build_kernel_core.yml:849` → ganti `--lto=none` ke `--lto=thin`
- **File:** `guidelines/03_OPTIMIZATION_LIMITS.md` → update deskripsi
- **Alasan:** Guidelines bilang DILARANG `--lto=none`, tapi workflow pake itu. Sekarang pake `--lto=thin` (lebih kecil, lebih cepet, aman di 7GB RAM).

### 4. `CONFIG_DEBUG_INFO_NONE` — Guidelines Diupdate
- **File:** `guidelines/03_OPTIMIZATION_LIMITS.md`
- **Masalah:** Guidelines nyuruh enable, tapi di A15 bikin BTF error → WiFi/data mati.
- **Fix:** Ditambah peringatan besar "JANGAN di A15!"

### 5. HZ Recommendation — Diperjelas
- **File:** `guidelines/03_OPTIMIZATION_LIMITS.md`
- **Fix:** `CONFIG_HZ=300` (default) vs HZ=1000 (gaming) — guidelines sekarang jelas.

---

## ⚠️ Yang Perlu Diingat

- **Kalau error "No build.log found" lagi:** Cek GitHub Actions langsung. Biasanya `repo sync` gagal atau toolchain download timeout.
- **`patches/` folder ga ada:** Cache key di `_build_kernel_core.yml` masih pake `hashFiles('patches/**', ...)` — kalo nanti bikin folder patches, cache bakal otomatis invalidate.
- **`tuning_profile` & `safety_profile`:** Masih TODO. Input dikirim tapi gak diproses.
- **Guidelines udah sync:** `01_KMI_AND_VERSIONING.md` udah OOP, `02_BAZEL_AND_MODULES.md` udah bener, `03_OPTIMIZATION_LIMITS.md` udah di-fix.

---

*Generated by opencode analysis — Last updated: 2026-05-16*
